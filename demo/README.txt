Demo theses.

These are built from public sources - company earnings releases and government
publications - so anyone can run audit.py without a paid research subscription.

Two of them are deliberately broken. Run:

    python audit.py --profile demo --framing neutral

and see whether it catches them. Answers are in the notes field of each file.
